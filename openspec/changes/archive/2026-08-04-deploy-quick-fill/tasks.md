## 1. 数据层：localStorage 存储工具

- [x] 1.1 定义历史记录数据结构和 localStorage key
- [x] 1.2 实现保存部署记录的函数（含去重、按 taskId 分组、最多 3 条）
- [x] 1.3 实现读取历史记录的函数（按 taskId 查询）

## 2. 组件层：Deploy.vue 集成

- [x] 2.1 在 Deploy.vue 中引入存储逻辑，添加相关响应式状态
- [x] 2.2 在 handleSubmit 中调用保存函数（API 调用之前）
- [x] 2.3 监听 taskId 变化，动态加载对应任务的历史记录

## 3. UI 层：快捷填充区域

- [x] 3.1 在表单顶部添加「快捷填充」区域（有记录时才显示）
- [x] 3.2 实现历史记录卡片样式（版本号、服务器数量、相对时间、构建号）
- [x] 3.3 实现点击回填逻辑（点击记录后填充表单所有字段）
- [x] 3.4 添加相对时间格式化工具函数（复用现有 formatBuildTime 思路）

## 4. 验证与构建

- [x] 4.1 验证功能：选择任务、填写表单、部署后刷新页面，确认记录存在
- [x] 4.2 验证功能：点击历史记录，确认所有字段正确回填
- [x] 4.3 验证功能：同任务部署超过 3 次，确认自动淘汰最旧
- [x] 4.4 验证功能：相同参数重复部署，确认不新增重复记录
- [x] 4.5 前端构建（npm run build）确保无编译错误
